# Public base URL the browser uses. Stays same-origin to avoid CORS;
# Next.js rewrites this prefix to BACKEND_API_URL on the server.
NEXT_PUBLIC_API_BASE_URL=/api/v1

# Server-side only. The actual backend the rewrite proxies to.
BACKEND_API_URL=http://localhost:8001/api/v1
